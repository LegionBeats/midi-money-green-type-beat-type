# 11 — Final CTA

Section with no id. Max-width 1040px, padding `clamp(40px,7vh,90px)` top, `clamp(24px,7vw,120px)` sides, `clamp(70px,11vh,130px)` bottom.

## Panel
The only card in the design with a linear gradient: `linear-gradient(180deg, #161616, #101010)`. `border: 1px solid rgba(255,255,255,.08)`, `border-radius: 28px` (largest radius in the design), padding `clamp(48px,8vw,90px)` vertical / 24px horizontal, `text-align: center`.

## Contents
**H2** — Archivo 900, uppercase, tracking `-.03em`, `line-height: .95`, `clamp(34px,6vw,76px)`, max-width 760px, `text-wrap: balance`. Copy: **Stop uploading like it's 2016.**

**Sub** — max-width 520px, `margin-top: 24px`, 17px, `line-height: 1.55`, `#8A8A85`. Copy: **Upload once. Video, beat store listing, checkout link in the description. Done.**

**Buttons** — `margin-top: 36px`, flex row, `gap: 14px`, centered, wrapping. Same two button styles as the hero band:
- **Start Selling Free** → `#top` (primary green)
- **See Plans** → `#pricing` (outline)

**Fine print** — `margin-top: 22px`, 13px, `#5A5A55`: **Free forever · No credit card required · Cancel anytime**

## Reveal
The whole panel reveals as one element, 0ms delay.
